# Build outputs
ssh_lib_build/
../ssh_lib_build/
obj/
lib/
bin/
*.ali
*.o
*.a
*.so
*.lexch
*.bexch

# Alire local state
alire/
alire/cache/
alire/builds/

# Alire-generated crate configuration. These are build artifacts: `alr update`
# regenerates them from each alire.toml. (They were tracked historically; the files
# are left in place, they are simply no longer followed.)
config/


# Editor/OS noise
*~
*.swp
.DS_Store

# Local live-test scratch should stay outside the repo; archived release
# evidence belongs under release_artifacts/.
/tmp/

# Transient test output (SSH-config fixtures written by the version-integration tests).
*.tmp
